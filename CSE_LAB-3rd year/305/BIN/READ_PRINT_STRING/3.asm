READ_STR PROC
PUSH AX
PUSH DX

CLD
XOR BX,BX
MOV AH,1
INT 21H

READ:
	CMP AL,0DH
	JE EXIT
	CMP AL,8H
	JNE STORE
	DEC BX
	DEC DI
	JMP INPUT
STORE:
	STOSB
	INC BX
INPUT:
	MOV AH,1
	INT 21H
	JMP READ
EXIT:
POP DX
POP AX
RET
READ_STR  ENDP 